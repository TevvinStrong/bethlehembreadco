import styled from "styled-components";

const StyledBlogFeedContainer = styled.div``;

//TODO: Create render function that maps over json data to render blog posts

const BlogFeed = () => {
  return(
      <StyledBlogFeedContainer>
        <>feed</>
      </StyledBlogFeedContainer>
  );
};

export default BlogFeed;